{
  "name": "finance-tracker-fullstack",
  "version": "1.0.0",
  "description": "Full-Stack Finance Tracker mit React Frontend und Node.js Backend",
  "scripts": {
    "dev": "bun run dev:concurrent",
    "dev:frontend": "cd frontend && bun dev",
    "dev:backend": "cd backend && bun run dev",
    "dev:concurrent": "concurrently \"bun run dev:backend\" \"bun run dev:frontend\"",
    "build": "bun run build:backend && bun run build:frontend",
    "build:frontend": "cd frontend && bun run build",
    "build:backend": "cd backend && bun run build",
    "start": "cd backend && bun start",
    "install:all": "bun install && cd frontend && bun install && cd ../backend && bun install",
    "clean": "rm -rf */node_modules */dist frontend/dist backend/dist",
    "lint": "cd frontend && bun run lint",
    "test": "cd backend && bun test && cd ../frontend && bun run test"
  },
  "keywords": [
    "finance",
    "tracker",
    "react",
    "typescript",
    "node.js",
    "express",
    "mongodb"
  ],
  "author": "Finance Tracker Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
